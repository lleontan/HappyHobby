class HobbyController {
  // Arguments - user, and 
  // Pre conditions - user logged in
  // Post conditions - Display daily hobby
  // Returns a JSON object with a hobby object containing hobby name, tags, description, and image
  getRandomHobby(user) {
    // grabs user's likes
    // Run random algorithm
    // Return a HobbyModel
  }
}