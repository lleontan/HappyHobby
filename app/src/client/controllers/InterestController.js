class InterestController {
  // Arguments - interest 
  // Pre conditions - user logged in
  // Post conditions - new interest object created
  // Returns a new JSON object with a list of interests
  createInterestObject(interest) {
    return {interests};
  }

  // Arguments - interest 
  // Pre conditions - user logged in
  // Post conditions - N/A
  sendInterestsToFirebase(interests) {
    // send JSON object with list of interests to Firebase
  }

  
}